#include <stdio.h>

int main(){
 
    int a = 1 , b = 0 ;
    printf("Operator Logika\n");
    printf("a && b = %d\n", a && b); // AND
    printf("a || b = %d\n", a || b); // OR
    printf("!a = %d\n", !a);       // NOT
    printf("!b = %d\n", !b);       // NOT

    return 0;
}